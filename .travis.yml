language: objective-c
xcode_project: CleanroomAppSettings.xcodeproj
osx_image: xcode8

git:
  depth: 1
  submodules: false

before_install:
  - git submodule update --init

install:
  - gem install xcpretty

script:
- set -o pipefail && xcodebuild -project CleanroomAppSettings.xcodeproj -scheme CleanroomAppSettings-iOS -destination 'platform=iOS Simulator,OS=10.0,name=iPhone 6s Plus' -destination-timeout 120 clean test | xcpretty
- set -o pipefail && xcodebuild -project CleanroomAppSettings.xcodeproj -scheme CleanroomAppSettings-OSX -destination 'platform=macOS' -destination-timeout 120 clean test | xcpretty
- set -o pipefail && xcodebuild -project CleanroomAppSettings.xcodeproj -scheme CleanroomAppSettings-tvOS -destination 'platform=tvOS Simulator,OS=10.0,name=Apple TV 1080p' -destination-timeout 120 clean test | xcpretty
- set -o pipefail && xcodebuild -project CleanroomAppSettings.xcodeproj -scheme CleanroomAppSettings-watchOS -destination 'platform=watchOS Simulator,OS=3.0,name=Apple Watch - 42mm' -destination-timeout 120 clean build | xcpretty

